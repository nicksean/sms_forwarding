# CLAUDE.md

This repository now uses the native ESP-IDF firmware path only. The former Arduino fallback sketch has been removed.

Use `AGENTS.md` as the authoritative engineering guide for:

- ESP-IDF build / flash / monitor commands
- component architecture
- Web asset generation
- extension conventions
- logging, privacy, modem, and SMS receive rules

Keep source comments in Chinese when editing firmware files.
